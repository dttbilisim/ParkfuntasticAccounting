@page "/cash-register"
@attribute [Authorize]
@inject NavigationManager Navigation
@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/cash-register-movements", replace: true);
}
